<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheet/s.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <h1 class="text-white  px-1 text-xl bg-blue-800">TASK MANAGER APP</h1>
        <div class="form p-10 text-white">
            <form action="/create" method="post">
                <input class="block  w-full px-5 py-2  rounded-md  outline-none  bg-zinc-800"  type="text" placeholder="title goes here." name="title">
                <textarea  class="block  w-full mt-2 px-5 py-2  rounded-md outline-none   resize-none  bg-zinc-800 "    name="details" id="" placeholder="write your tasks"></textarea>
                 <input  class="mt-2 bg-blue-600 px-5 py-2 rounded-md"   type="submit" value="Creat tasks">

            </form>
        </div>
        <div class="tasks  flex gap-3 flex-wrap  p-10">
            <% files = files || []; %>  <!-- Ensures files is at least an empty array -->
            <% if (files.length > 0) { %>
                <% files.forEach(function(val) { %>
                    <div class="task min-w-72 px-3 py-4 rounded-md bg-zinc-800">
                        <h1 class="text-white text-3xl tracking-tighter"><%= val %></h1>
                        <div class="flex w-full justify-between mt-2">
                            <a class="text-blue-800 inline-block" href="/file/<%= val %>">Read More.</a>
                            <a class="text-zinc-600" href="/edit/<%= val %>">Edit Filename</a>
                            <a class="text-zinc-500" href="/del/<%= val %>">Delete</a>
                        </div>
                    </div>
                <% }); %>
            <% } else { %>
                <h1 class="text-zinc-600 text-white">No tasks yet.</h1>
            <% } %>
            
         

             </div>

             
    
    </div>
</body>
</html>
